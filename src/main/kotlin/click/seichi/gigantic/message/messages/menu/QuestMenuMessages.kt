package click.seichi.gigantic.message.messages.menu

import click.seichi.gigantic.message.LocalizedText
import java.util.*

/**
 * @author tar0ss
 */
object QuestMenuMessages {

    val SELECT_MENU_TITLE = LocalizedText(
            Locale.JAPANESE to "クエスト一覧"
    )

    val MONSTER_LIST = LocalizedText(
            Locale.JAPANESE to "出現モンスター"
    )

    val MONSTER_REASON = LocalizedText(
            Locale.JAPANESE to "ブロック破壊時に低確率で出現"
    )

    val PROCESS_ON = LocalizedText(
            Locale.JAPANESE to "クリックで一時中断"
    )

    val PROCESS_OFF = LocalizedText(
            Locale.JAPANESE to "クリックで受注"
    )

}