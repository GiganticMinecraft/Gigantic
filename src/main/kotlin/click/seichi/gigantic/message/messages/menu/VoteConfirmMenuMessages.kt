package click.seichi.gigantic.message.messages.menu

import click.seichi.gigantic.message.LocalizedText
import java.util.*

/**
 * @author Mr_IK
 */
object VoteConfirmMenuMessages {

    val TITLE = LocalizedText(
            Locale.JAPANESE to "投票特典受け取り 確認画面"
    )

    val CLICK_TO_OK = LocalizedText(
            Locale.JAPANESE to "クリックで受け取る"
    )

    val NOTHING_RESULT = LocalizedText(
            Locale.JAPANESE to "特典はすでに受け取り済みです"
    )

    val GET_RESULT = LocalizedText(
            Locale.JAPANESE to "特典を受け取りました"
    )

    val CLICK_TO_NG = LocalizedText(
            Locale.JAPANESE to "クリックで戻る"
    )
}