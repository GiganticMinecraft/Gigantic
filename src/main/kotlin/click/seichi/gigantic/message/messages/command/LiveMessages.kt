package click.seichi.gigantic.message.messages.command

import click.seichi.gigantic.message.LocalizedText
import java.util.*

/**
 * @author tar0ss
 */
object LiveMessages {

    val ON = LocalizedText(
            Locale.JAPANESE to "ライブモードをONにしました"
    )

    val OFF = LocalizedText(
            Locale.JAPANESE to "ライブモードをOFFにしました"
    )

}